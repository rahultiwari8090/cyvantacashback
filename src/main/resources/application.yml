spring:
  application:
    name: affiliate-app
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/affiliate_db}
server:
  port: 8080
logging:
  level:
    root: INFO
    com.cyvanta.affiliate_app: DEBUG
